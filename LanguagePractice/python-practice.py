# Reverse vs Reversed
'''
lst.reverse() -> reverses lst in place and returns None
reversed(lst) -> returns an iterator that access the elements in reverse order
list(reversed(lst)) -> returns reversed of lst without modifying the original lst
'''

# initialize list of size n
output = [0] * len(lst)  # this is how you init list of size n
